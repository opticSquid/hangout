apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: persistance
  labels:
    app: postgres
type: Opaque
data:
  # this is the base64 encoded version of postgres
  # base64 encoding is required
  POSTGRES_USER: cG9zdGdyZXM=
  # this is the base64 encoded version of PkNLm@#2357
  # base64 encoding is required
  POSTGRES_PASSWORD: UGtOTG1AIzIzNTc=
  # this is the base64 encoded version of replicator
  # base64 encoding is required
  POSTGRES_REPLICATION_USER: cmVwbGljYXRvcg==
  # this is the base64 encoded version of Vf4PgProFXOS
  # base64 encoding is required
  POSTGRES_REPLICATION_PASSWORD: VmY0UGdQcm9GWE9T
